export class Res {
    static from(source, transformer) {
        return transformer.transform(source);
    }
}
export class ResMut {
    static from(source, transformer) {
        return transformer.transform(source);
    }
}
